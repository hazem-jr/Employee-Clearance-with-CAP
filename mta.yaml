_schema-version: 3.3.0
ID: EmployeeClearance
description: A simple CAP project.
version: 1.0.0
modules:
- name: EmployeeClearance-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: EmployeeClearance-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: EmployeeClearance-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: EmployeeClearance-db
  parameters:
    buildpack: nodejs_buildpack
- name: EmployeeClearance-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: EmployeeClearance-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - clearance.zip
      name: clearance
      target-path: resources/
    - artifacts:
      - clearance001.zip
      name: clearance001
      target-path: resources/
- name: clearance
  type: html5
  path: app/clearance
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: clearance001
  type: html5
  path: app/clearance001
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: EmployeeClearance-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: EmployeeClearance-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: EmployeeClearance-html5-service
    service-plan: app-host
- name: EmployeeClearance-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: EmployeeClearance-destination-service
    service-plan: lite
  requires:
  - name: srv-api
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
